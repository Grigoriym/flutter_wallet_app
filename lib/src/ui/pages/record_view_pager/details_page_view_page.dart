import 'package:flutter/material.dart';

class DetailsPageViewPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}